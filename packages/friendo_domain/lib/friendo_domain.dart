/// The friendO domain core.
///
/// Everything here is pure Dart. There is no Flutter, no clock, no file access
/// and no database. A function that needs the current time takes it as an
/// argument.
library;

export 'src/phase.dart';
